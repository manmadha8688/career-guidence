# /back-debug

Debug any backend/API issue — 401/403/500, slow response, stale data.

Read `.claude/commands/back-debug.md` and execute that workflow fully, end to end, applied to the issue in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
