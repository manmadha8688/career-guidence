# /front-audit

Deep frontend production audit — bugs, race conditions, edge cases, auth.

Read `.claude/commands/front-audit.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
