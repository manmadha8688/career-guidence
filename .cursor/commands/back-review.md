# /back-review

Full backend code review — structure, cache, N+1, security.

Read `.claude/commands/back-review.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
