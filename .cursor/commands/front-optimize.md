# /front-optimize

Frontend performance — bundle size, lazy loading, cache, re-renders.

Read `.claude/commands/front-optimize.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
