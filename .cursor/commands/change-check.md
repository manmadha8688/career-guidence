# /change-check

Targeted verification after a small change.

Read `.claude/commands/change-check.md` and execute that workflow fully, end to end, applied to the change in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
