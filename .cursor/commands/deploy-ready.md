# /deploy-ready

Full deployment readiness check — Vercel + Render, latest limits, env vars, CORS, smoke test.

Read `.claude/commands/deploy-ready.md` and execute that workflow fully, end to end. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` + `.claude/skills/deployment/` for relevant detail.
