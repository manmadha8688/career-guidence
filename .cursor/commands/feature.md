# /feature

Feature build workflow.

Read `.claude/commands/feature.md` and execute that workflow fully, end to end, for the feature in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
