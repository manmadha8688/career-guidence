# /bugfix

Structured bug-fix workflow.

Read `.claude/commands/bugfix.md` and execute that workflow fully, end to end, applied to the bug in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
