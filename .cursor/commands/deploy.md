# /deploy

Deployment workflow.

Read `.claude/commands/deploy.md` and execute that workflow fully, end to end. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and consult `.claude/skills/deployment/` + `.claude/workflows/deployment.md`.
