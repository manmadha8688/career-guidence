# /back-optimize

Backend performance — queries, cache coverage, response size.

Read `.claude/commands/back-optimize.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
