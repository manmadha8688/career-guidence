# /back-fix

Fix any Spring Boot issue end-to-end — 401/403/500, slow, stale data, Atlas, CORS, cache, quiz/XP.

Read `.claude/commands/back-fix.md` and execute that workflow fully, end to end, applied to the issue in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
