# /front-review

Full frontend code review — structure, imports, architecture, naming.

Read `.claude/commands/front-review.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
