# /scaffold

Scaffold new code/module following project structure.

Read `.claude/commands/scaffold.md` and execute that workflow fully, end to end, for the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
