# /front-fix

Fix any React issue end-to-end — crash, blank page, loading, state, quiz, auth, theme, routing, modals.

Read `.claude/commands/front-fix.md` and execute that workflow fully, end to end, applied to the issue in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
