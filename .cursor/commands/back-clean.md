# /back-clean

Backend dead code — unused endpoints, orphan methods, stale DTOs.

Read `.claude/commands/back-clean.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
