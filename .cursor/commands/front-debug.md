# /front-debug

Debug any frontend/UI issue — crash, loading, state, auth, navigation.

Read `.claude/commands/front-debug.md` and execute that workflow fully, end to end, applied to the issue described in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
