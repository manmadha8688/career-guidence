# /team-review

Multi-agent team review (architecture, frontend, backend, security, performance).

Read `.claude/commands/team-review.md` and execute that workflow fully, end to end, applied to the target in my message. Use the relevant Cursor subagents (defined in `.claude/agents/`). Honor `.cursor/rules/` and check `.claude/memory/MEMORY.md`.
