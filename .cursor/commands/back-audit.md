# /back-audit

Deep backend production audit — correctness, baselines, edge cases.

Read `.claude/commands/back-audit.md` and execute that workflow fully, end to end, applied to the target in my message. Honor `.cursor/rules/` (which bridge to `.claude/rules/`) and check `.claude/memory/MEMORY.md` for relevant feedback.
